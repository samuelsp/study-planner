# ==============================================
# Study Planner - Docker Environment Variables
# ==============================================

# Backend Configuration
# ---------------------
# Porta do servidor backend (padrão: 4000)
PORT=4000

# URL de conexão do banco de dados
# Para Docker em produção, use: file:/data/prod.db
# Para Docker em desenvolvimento, use: file:/data/dev.db
# Para desenvolvimento local, use: file:./dev.db
DATABASE_URL="file:./dev.db"

# Ambiente de execução (development | production)
NODE_ENV=development

# Frontend Configuration (Vite)
# ------------------------------
# URL da API backend para o frontend
# Em desenvolvimento local: http://localhost:4000
# Em produção Docker: http://backend:4000 (comunicação interna)
VITE_API_URL=http://localhost:4000

# Email Configuration (Opcional - para notificações)
# --------------------------------------------------
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=seu-email@gmail.com
# EMAIL_PASS=sua-senha-ou-app-password
# EMAIL_FROM=Study Planner <noreply@studyplanner.com>

# ==============================================
# Instruções:
# ==============================================
# 1. Copie este arquivo para .env na raiz do projeto
# 2. Para desenvolvimento local (sem Docker):
#    - Use DATABASE_URL="file:./dev.db"
#    - Use VITE_API_URL=http://localhost:4000
#
# 3. Para Docker (docker-compose cuida automaticamente):
#    - DATABASE_URL será sobrescrito no docker-compose.yml
#    - VITE_API_URL será configurado automaticamente
#
# 4. Não commite o arquivo .env com dados sensíveis!
# ==============================================
